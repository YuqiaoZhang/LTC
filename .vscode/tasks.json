{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-linux-x11-x64-debug",
            "type": "shell",
            "command": "${workspaceFolder}/bldsys/posix_linux_x11/build.sh x64 debug",
            "group": {
                "kind": "build",
            }
        },
        {
            "label": "build-vulkan-glsl",
            "type": "shell",
            "command": "${workspaceFolder}/bldsys/vulkan_glsl/build.sh",
            "group": {
                "kind": "build",
            }
        }
    ]
}